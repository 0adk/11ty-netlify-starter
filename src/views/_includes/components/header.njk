<header class="header">
  <div class="container header-wrap">
    <a href="/" class="logo">
      <img src="/assets/img/icons/plant.png">
    </a>
    <nav>
      <ul class="nav-list">
        {# {% for link in nav %}
          <li><a href="{{link.href}}" class="nav-list-link">{{link.name}}</a></li>
        {% endfor %} #}
         {% for link in collections.pages %}
        <li>
            <a href="{{ link.url }}" class="nav-list-link">{{ link.fileSlug | capitalize }}</a>
        </li>
          {% endfor %}
      </ul>
    </nav>
  </div>
</header>
